<h1> Index Page</h1>

<%= button_to  "Add New Session",new_session_path,method: :get%>   

<% @sessions.each do |session|%>
    <h1><%= link_to  session.session_name, show_session_url(session)%></h1>
<% end %>