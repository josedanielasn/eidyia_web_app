<h1> Batch Index</h1>
 
 <%= button_to  "Add Batch",new_batch_path, :class => "btn btn-primary btn-md",method: :get%>   
  
    <% @batch.each do |batch| %>
            <li><%= link_to batch.batch_name, show_batch_url(batch)%></li>
    <%end%>